[pytest]
markers =
    integration: tests that call the live CloudPSS API
    slow_emt: expensive live EMT study regressions kept outside the default fast live layer
    smoke: lightweight tests that only verify imports, instantiation, or basic attributes
    needs_improvement: tests that pass but need stronger business-logic assertions
